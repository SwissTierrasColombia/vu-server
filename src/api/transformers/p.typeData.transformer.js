import Transformer from 'transformer-response-data';

//create transformer and define output properties
export let pTypeDataTransformer = new Transformer({
    "_id": "_id",
    "typeData": "typeData"
});